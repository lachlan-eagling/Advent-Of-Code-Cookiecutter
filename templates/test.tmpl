from unittest import TestCase
from challenges import day_{{ day }} as challenge
from utils import load_solution


class TestDay{{ day }}(TestCase):

    def __init__(self):
        self.solution = load_solution({{ day }})
        super().__init__()

    def test_part_1(self):
        result = challenge.part1()
        self.assertEqual(result, self.solution)

    def test_part_2(self):
        result = challenge.part1()
        self.assertEqual(result, self.solution)